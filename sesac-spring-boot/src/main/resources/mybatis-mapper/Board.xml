<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sesac.sesacspringboot.mapper.BoardMapper">


        <!-- 게시글 작성 쿼리 -->
        <insert id="createBoard" parameterType="sesac.sesacspringboot.domain.Board">
            INSERT INTO board (title, content, writer) VALUES (#{title}, #{content}, #{writer})
        </insert>

<!--    <select id="boardAll" resultType="sesac.sesacspringboot.domain.Board">-->
<!--        SELECT board.* FROM board-->
<!--    </select>-->

<!--    &lt;!&ndash; 검색어에 해당하는 제목의 개수를 가져오는 쿼리 &ndash;&gt;-->
<!--    <select id="getMatchingTitleCount" parameterType="string" resultType="int">-->
<!--        SELECT COUNT(*) FROM board WHERE title LIKE #{word}-->
<!--    </select>-->

<!--    &lt;!&ndash; 전체 게시글 개수를 가져오는 쿼리 &ndash;&gt;-->
<!--    <select id="getTotalBoardCount" resultType="int">-->
<!--        SELECT COUNT(*) FROM board-->
<!--    </select>-->

<!--    &lt;!&ndash; 게시글 작성 쿼리 &ndash;&gt;-->
<!--    <insert id="insertBoard" parameterType="com.example.model.Board">-->
<!--        INSERT INTO board (title, content, writer) VALUES (#{title}, #{content}, #{writer})-->
<!--    </insert>-->

<!--    &lt;!&ndash; 게시글 수정 쿼리 &ndash;&gt;-->
<!--    <update id="updateBoard" parameterType="com.example.model.Board">-->
<!--        UPDATE board SET title = #{title}, content = #{content}, writer = #{writer} WHERE id = #{id}-->
<!--    </update>-->

<!--    &lt;!&ndash; 게시글 삭제 쿼리 &ndash;&gt;-->
<!--    <delete id="deleteBoard" parameterType="int">-->
<!--        DELETE FROM board WHERE id = #{id}-->
<!--    </delete>-->

</mapper>
